<%= form_for(shipping_option) do |f| %>
  <% if shipping_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipping_option.errors.count, "error") %> prohibited this shipping_option from being saved:</h2>

      <ul>
      <% shipping_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="field">
    <%= f.label :is_local_pickup %>
    <%= f.check_box :is_local_pickup %>
  </div>

  <div class="field">
    <%= f.label :is_deleted %>
    <%= f.check_box :is_deleted %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
